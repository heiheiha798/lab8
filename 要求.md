## 项目说明

* 一切手段都可以用，但要满足以下条件：
    * 所有输入的操作数均为 **SINT8**。
    * 输入矩阵维度均为 **512×512**。
* 基本加速器框架为哈佛架构， **imem** 和 **omem** 分离，单接口 **64位宽**。
    * 生成伪随机输入 **input_mem.csv** (结构是前 **512*512/8** 个地址为第一个矩阵，地址 **23’d32768** 开始为第二个 **512*512*INT8** 矩阵)。
    * Input Memory 与 Result Memory 的数据排布方式由你决定，但起始输入排布和最终检查结果会用 **make generate_input** 与 **make check_result** 进行。中间数据重新排布可以用 C++/Python 软件程序的方式进行（按需自己写）。
* 关于片上 Buffer:
    * 全世界只有一个 **1ns~10s** 的时钟。注意，是仅能用一个时钟，但是它的频率可以在 **1ns~10ns** 之间任意选择，精确到小数点后 **2位**。
    * SRAM memory 读延迟 **<1ns**。SRAM 可选择的参数 (IO bit width, 可以存多少个 word，以及对应的面积和功耗) 在 **SRAM_available_specs** 里。如果想用更多地址、多大空间，请用提供的这些小模块自行组合。
    * 功耗计算为：**动态功耗(W/Hz) \* 工作频率(Hz) + 静态功耗(W)**。
* 芯片规模与精度：
    * 虽然理论上可以做任意规模(如 **10000**)的 MAC 乘加，但注意 TPUv4 中每个 unit 也就 **16×16×16=4096** 个乘法器，华为昇腾单个计算核也不过 **16×8** 的维度，不要一开始就做太多乘加器，否则太大了综合不出来。
    * 输入有 **35%** 的稀疏度（**35%** 的零），请考虑中间计算结果需要用 **BF16** 还是直接用 **long INT**。

---
## 项目要求

* 计算功能正确。
* 在计算功能正确的情况下，综合得分 **Score** 越低越好。

---
## 评分标准

* 计算功能正确占此部分的 **70%**。
* 在计算功能正确的情况下，综合得分 Score 计算公式为：
    * $Score = \exp(NMSE/C0) \times 功率_{power}(unit:mW) \times (面积_{area}(unit:um^2))^2 \times (时间(us))$
    * 其中相对均方误差 Normalized Mean-Squared Error（**NMSE**）$= \sum(((计算结果每个element - 正确无损计算结果每个element)/正确无损计算结果每个element)^2)$，其中 **C0=1E-3** (这个值是为了让 BF16 产生的 **1E-5** 的 SSE 影响整体分数在增加 **1%~5%** 左右，无损计算 SSE=0 时第一项应该为 1)。
    * **latency** 计算方法为从 **comp_enb** 的下降沿开始计算，到 **busyb** 的上升沿的绝对时间 (单位：**ns**)。也可以是 **cycle number × shortest clock period (target freq-slack)**。

---
## 逻辑综合要求

* 逻辑综合后 **critical path setup time slack > 0**，对应设置的主频。
* 用到的 **DFF** 的 PPA 都在 syn report 里，用到的 **SRAM macro** 需要单独算。
* 综合时请剔除 **SRAM macro** (将 SRAM port 拉到顶层, 待 logic syn 的 design 不应包含 SRAM instances)，不然会很大。
* **input memory**、 **result memory** 与 **top controller** 不计入 PPA 计算。
* **Top Controller**、 **Input Memory** 与 **Data Memory** 不计入最终 PPA 评分，只看 **accelerator**。

---
## 最终提交

* 设计报告 (自己计算一下上面的得分)
* 设计与测试代码
* 综合用脚本 **syn.tcl**
* PPA 原始报告 (top level)